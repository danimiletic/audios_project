import Image from 'react-bootstrap';

const Home = ({}) => (
  <>
    <h1>Welcome to Audios!</h1>
    <h3>As you can see, we're all about ~simplicity~</h3>
    <p>Hence, the empty home page.</p>
    <p>Let's not clutter more than we have to!</p>
  </>
)

export default Home;